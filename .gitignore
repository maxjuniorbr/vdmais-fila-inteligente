node_modules/
dist/
coverage/
playwright-report/
test-results/
.env
.env.*
!.env.example
*.log

# TypeScript incremental build cache
*.tsbuildinfo

# Private keys / certificates (never commit; integration RS256 keys live in .env)
*.pem
*.key

# Claude Code (MCP token + personal local settings)
.mcp.json
.claude/settings.local.json

# SonarQube local
.scannerwork/
.sonarlint/

# Editor settings
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Local test credentials (plaintext — never version)
/docs/credenciais-teste-local.md
/docs/credenciais-teste-supabase.md

# Apigee onboarding spec draft (will hold corporate gateway values — never version)
/docs/template-apigee.yaml
